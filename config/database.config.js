const config = {
    db_name : "ukk_hotel",
    username : "root",
    password : "",
    dbms : {
        host: 'localhost',
        dialect: 'mysql'
    },
    logging : true, //log setiap query database ke konsole
}

module.exports = {config}